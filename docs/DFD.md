# 🔄 Data Flow Diagrams (DFD) — SafeStep

> This document contains Level 0 (Context), Level 1 (System), and Level 2 (Process Detail) Data Flow Diagrams for the SafeStep Women Safety Application.

---

## Level 0 — Context Diagram (Highest Level)

```
                    ┌─────────────────────────────────────────┐
                    │                                         │
  ┌──────────┐      │            ╔═══════════╗               │     ┌─────────────────────┐
  │          │─────▶│  App Input ║           ║ SMS + Location│────▶│ Emergency Contacts  │
  │   USER   │      │            ║ SAFESTEP  ║               │     │ (Family / Friends)  │
  │          │◀─────│  Feedback  ║   SYSTEM  ║               │     └─────────────────────┘
  └──────────┘      │            ╚═══════════╝               │
                    │                   │                     │     ┌─────────────────────┐
                    │                   │ Auth, Data Sync     │────▶│   Firebase Cloud    │
                    │                   │                     │     │ (Auth + Firestore)  │
                    │                   │                     │     └─────────────────────┘
                    │                   │                     │
                    │                   │ Location Request    │     ┌─────────────────────┐
                    │                   │                     │────▶│   GPS / Google      │
                    │                   │                     │     │   Location API      │
                    └─────────────────────────────────────────┘     └─────────────────────┘

External Entities:
  • USER               — Woman using the app
  • Emergency Contacts — Pre-configured trusted contacts receiving SOS SMS
  • Firebase Cloud     — Authentication server + Firestore database
  • GPS / Location API — Android GPS + optional geocoding API
```

---

## Level 1 — System Level DFD

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                              SAFESTEP — Level 1 DFD                                      │
└──────────────────────────────────────────────────────────────────────────────────────────┘

  USER
   │
   ├──[credentials]────────────────▶ ┌──────────────────────┐
   │                                 │  P1: AUTHENTICATION   │──[user profile]──▶ Firestore
   │◀──[auth token / error]──────────└──────────────────────┘
   │
   ├──[trigger: voice/shake/button]─▶ ┌──────────────────────┐
   │                                  │ P2: TRIGGER DETECTION │
   │                                  └──────────────────────┘
   │                                           │
   │                                           │ [trigger_event]
   │                                           ▼
   │                                  ┌──────────────────────┐      ┌──────────────────┐
   │                                  │  P3: ALERT PROCESSOR  │◀─────│  D1: Contacts DB  │
   │                                  └──────────────────────┘      │  (SharedPrefs /  │
   │                                           │                     │   Hive / Firebase)│
   │                                           │                     └──────────────────┘
   │                           ┌──────────────┼───────────────┐
   │                           ▼              ▼               ▼
   │                  ┌──────────────┐ ┌───────────┐ ┌─────────────────┐
   │                  │P4: GPS/LOC   │ │P5: SMS    │ │P6: AUDIO RECORD │
   │                  │  SERVICE     │ │  SERVICE  │ │  SERVICE        │
   │                  └──────────────┘ └───────────┘ └─────────────────┘
   │                          │               │               │
   │                          │ [coordinates] │ [SMS sent]    │ [audio file]
   │                          ▼               ▼               │
   │                  GPS Hardware     Emergency             D2: Local Storage
   │                                   Contacts
   │
   ├──[contact CRUD]────────────────▶ ┌──────────────────────┐
   │                                  │ P7: CONTACT MANAGER  │──[sync]──▶ Firestore
   │                                  └──────────────────────┘
   │
   └──[settings change]─────────────▶ ┌──────────────────────┐
                                       │ P8: SETTINGS MANAGER │──[store]──▶ SharedPrefs
                                       └──────────────────────┘

Data Stores:
  D1: Contacts DB  — SharedPreferences + Hive + Firebase Firestore
  D2: Local Storage — Device file system (audio recordings)
```

---

## Level 2 — Process P2: Trigger Detection (Detailed)

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                       P2: TRIGGER DETECTION — Level 2 DFD                                │
└──────────────────────────────────────────────────────────────────────────────────────────┘

                         ┌─────────────────────┐
                         │  EmergencyDetector   │
                         │  (Orchestrator)      │
                         └─────────────────────┘
                          /         |          \
                         /          |           \
                        ▼           ▼            ▼
          ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐
          │ P2.1: SHAKE  │  │ P2.2: VOICE  │  │ P2.3: BUTTON     │
          │  DETECTOR    │  │  SERVICE     │  │  DETECTOR        │
          └──────────────┘  └──────────────┘  └──────────────────┘
                │                  │                   │
                │                  │                   │
                ▼                  ▼                   ▼

  ┌────────────────────┐  ┌─────────────────────┐  ┌────────────────────────┐
  │ Accelerometer      │  │ Microphone Stream   │  │ Volume Button Stream   │
  │ Event Stream       │  │ (speech_to_text)    │  │ (PerfectVolumeControl) │
  └────────────────────┘  └─────────────────────┘  └────────────────────────┘
          │                        │                          │
          ▼                        ▼                          ▼
  ┌────────────────────┐  ┌─────────────────────┐  ┌────────────────────────┐
  │ Check thresholds   │  │ Compare recognized  │  │ Count press events     │
  │ X/Y/Z > threshold? │  │ words vs trigger    │  │ 5× rapid = trigger     │
  │ (from SharedPrefs) │  │ list (SharedPrefs)  │  │ or hold = trigger      │
  └────────────────────┘  └─────────────────────┘  └────────────────────────┘
          │                        │                          │
          └────────────────────────┴──────────────────────────┘
                                   │
                                   ▼
                         ┌─────────────────────┐
                         │  trigger_event =    │
                         │  {type, timestamp}  │
                         └─────────────────────┘
                                   │
                                   ▼
                           → P3: ALERT PROCESSOR


  P2.2: Voice Service — Internal State Machine:
  ─────────────────────────────────────────────
  
    [IDLE] ──init()──▶ [INITIALIZED] ──startListening()──▶ [LISTENING]
                                                                │
                              ┌─────────────────────────────────┘
                              │
                    status="done"/"error"
                              │
                              ▼
                    [SCHEDULED RESTART] ─▶ check voice_enabled pref
                              │
                    ┌─────────┴───────────┐
                    ▼                     ▼
              [LISTENING]           [STOPPED] (if voice_enabled=false)
              (auto-restart)
```

---

## Level 2 — Process P3: Alert Processor (Detailed)

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                       P3: ALERT PROCESSOR — Level 2 DFD                                  │
└──────────────────────────────────────────────────────────────────────────────────────────┘

                ┌──────────────────────────────────────┐
                │         AlertService.triggerAlert()   │
                └──────────────────────────────────────┘
                                  │
                    ┌─────────────▼────────────────┐
                    │ 1. Check _isAlertInProgress   │
                    │    (prevent double triggers)   │
                    └─────────────┬────────────────┘
                                  │ [not in progress]
                    ┌─────────────▼────────────────┐
                    │ 2. Show SOS snackbar in UI    │
                    └─────────────┬────────────────┘
                                  │
              ┌───────────────────┼──────────────────────┐
              ▼                   ▼                       ▼
  ┌─────────────────┐  ┌──────────────────┐  ┌────────────────────┐
  │ P3.1: Load User │  │ P3.2: Get GPS    │  │ P3.3: Start Audio  │
  │ & Contacts from │  │ Coordinates      │  │ Recording          │
  │ SharedPreferences│  │ (geolocator)    │  │ (record package)   │
  └─────────────────┘  └──────────────────┘  └────────────────────┘
              │                   │
              │ [contacts list]   │ [lat, lng]
              ▼                   ▼
  ┌───────────────────────────────────────────────┐
  │ P3.4: Build SMS Message                       │
  │                                               │
  │ "🚨 EMERGENCY! [Name] needs help!             │
  │  Location: [address]                          │
  │  Google Maps: maps.google.com/?q=lat,lng      │
  │  Time: [timestamp]"                           │
  └───────────────────────────────────────────────┘
                             │
                             ▼
  ┌───────────────────────────────────────────────┐
  │ P3.5: For each contact → SMSService.send()    │
  │    → Native Android SMS dispatch              │
  │    → Log send status (success/fail)           │
  └───────────────────────────────────────────────┘
                             │
                             ▼
  ┌───────────────────────────────────────────────┐
  │ P3.6: Open SOS Status Tracker screen in UI    │
  │    (show live SMS delivery statuses)          │
  └───────────────────────────────────────────────┘
                             │
                             ▼
  ┌───────────────────────────────────────────────┐
  │ P3.7: Reset _isAlertInProgress after timeout  │
  └───────────────────────────────────────────────┘
```

---

## Level 2 — Process P1: Authentication (Detailed)

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                           P1: AUTHENTICATION — Level 2 DFD                               │
└──────────────────────────────────────────────────────────────────────────────────────────┘

  USER
   │
   ├──[email + password]──▶ ┌────────────────────────┐
   │                        │  P1.1: REGISTER        │──▶ Firebase Auth (createUser)
   │◀──[success / error]────└────────────────────────┘       │
   │                                                           │ [new uid]
   │                                                           ▼
   │                                                  ┌────────────────────┐
   │                                                  │ P1.2: CREATE USER  │──▶ Firestore
   │                                                  │ PROFILE in DB      │   users/{uid}
   │                                                  └────────────────────┘
   │
   ├──[email + password]──▶ ┌────────────────────────┐
   │                        │  P1.3: LOGIN           │──▶ Firebase Auth (signIn)
   │◀──[auth token / error]─└────────────────────────┘
   │                                  │
   │                                  │ [uid]
   │                                  ▼
   │                         ┌────────────────────────┐
   │                         │  P1.4: LOAD USER DATA  │◀── Firestore (users/{uid})
   │                         └────────────────────────┘
   │                                  │
   │                                  │ [user model + contacts]
   │                                  ▼
   │                         ┌────────────────────────┐
   │                         │  P1.5: CACHE TO        │──▶ SharedPreferences
   │◀──[navigate to Home]────│  SharedPreferences     │   (cached_user_name,
   │                         └────────────────────────┘    cached_contacts)


  AuthWrapper Navigation Logic:
  ─────────────────────────────
  App Start
      │
      ▼
  StreamBuilder (Firebase authStateChanges)
      │
      ├── [User == null] ──▶ AuthScreen (Login/Register)
      │
      └── [User != null] ──▶ HomeScreen (Dashboard)
```

---

## Level 2 — Process P7: Contact Manager (Detailed)

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                         P7: CONTACT MANAGER — Level 2 DFD                                │
└──────────────────────────────────────────────────────────────────────────────────────────┘

  USER
   │
   ├──[open contacts screen]──▶ ┌─────────────────────────────┐
   │                            │ P7.1: LOAD CONTACTS         │
   │                            │  1. Read from Firestore     │
   │◀──[contact list]───────────│  2. Fallback: SharedPrefs   │
   │                            └─────────────────────────────┘
   │
   ├──[browse phone book]──────▶ ┌─────────────────────────────┐
   │                             │ P7.2: PHONE CONTACT PICKER  │
   │◀──[selected contact]────────│  (flutter_contacts library)  │
   │                             └─────────────────────────────┘
   │
   ├──[tap "Save"]─────────────▶ ┌─────────────────────────────┐
   │                             │ P7.3: SAVE CONTACTS         │
   │                             │                             │
   │                             │ Step 1: SharedPreferences   │──▶ cached_contacts (JSON)
   │                             │ Step 2: Firebase Firestore  │──▶ users/{uid}
   │                             │ Step 3: Hive (local cache)  │──▶ contacts box
   │◀──[success / error]─────────└─────────────────────────────┘
   │
   └──[delete contact]─────────▶ ┌─────────────────────────────┐
                                  │ P7.4: DELETE CONTACT        │
                                  │  Remove from all 3 stores   │
                                  └─────────────────────────────┘

  Data Flow Priority (Read):
    1. Firebase Firestore (most up-to-date, requires internet)
    2. SharedPreferences (fast, works offline + in background isolates)
    3. Hive (backup for background isolates)
```

---

## Data Flow Summary Table

| Process | Input | Processing | Output | Storage Read | Storage Write |
|---|---|---|---|---|---|
| P1: Auth | Email+Password | Firebase Auth verify | Auth token / Error | Firebase Auth | SharedPrefs (cache) |
| P2: Trigger Detection | Sensor streams | Threshold compare / Word match | trigger_event | SharedPrefs (settings) | — |
| P3: Alert Processor | trigger_event | Build SMS, get GPS | SMS sent, UI updated | SharedPrefs (contacts) | — |
| P4: GPS Service | — | Geolocator.getCurrentPosition | lat, lng, address | — | — |
| P5: SMS Service | Message + contacts | Platform channel → Android SMS | Delivery status | — | — |
| P6: Audio Recording | — | record package → .m4a | Audio file path | — | Device filesystem |
| P7: Contact Manager | User input | CRUD contacts | Updated contact list | Firestore + SharedPrefs | Firestore + SharedPrefs + Hive |
| P8: Settings Manager | Toggle/slider input | Write bool/double | Settings saved | SharedPrefs | SharedPrefs |

---

*Document generated: February 2026 | SafeStep v1.0.0*
